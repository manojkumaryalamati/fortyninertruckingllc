You are a senior full-stack engineer. Fix my site code so Admin can create a driver and upload multiple documents to Firebase Storage without getting 403 (storage/unauthorized).

Context:
- Error: Firebase Storage: User does not have permission to access 'driver_documents/<driverId>/DL/<timestamp>_<file>.pdf' (storage/unauthorized)
- Admin creates drivers with documents during creation.
- Storage path should be dynamic: driver_documents/{driverId}/{docType}/{timestamp}_{originalName}
- After upload, save file metadata + downloadURL into Firestore under the driver record.

Your tasks (do all):
1) Inspect the project and locate:
   - Firebase initialization (firebase.ts/js)
   - Auth flow (login/admin session)
   - Driver create flow (add driver)
   - Upload code (uploadBytesResumable/uploadBytes, getDownloadURL)
   - Firestore driver schema (drivers collection) and any users/roles logic.

2) Fix the upload logic so it is NOT hardcoded:
   - First create the driver in Firestore and capture driverId (docRef.id).
   - For each selected file, determine docType (DL / MED / AGREEMENT / etc).
   - Build fullPath = `driver_documents/${driverId}/${docType}/${Date.now()}_${sanitize(file.name)}`
   - Upload to Storage using uploadBytesResumable.
   - Get downloadURL, then write metadata to Firestore:
     Example structure:
       drivers/{driverId}
         documents: [
           { docType, fileName, fullPath, downloadURL, uploadedAt, uploadedBy }
         ]
   - Add strong error handling + UI logs for each step (auth, driver create, each file upload).

3) Ensure auth is guaranteed during upload:
   - If auth.currentUser is null, block upload and show “Please sign in”.
   - Wait for onAuthStateChanged before allowing the admin form to submit.
   - Log uid + token claims (getIdTokenResult) to confirm admin status.

4) Fix Firebase Storage Rules in the repo (firebase.storage.rules or storage.rules):
   Implement ONE of these two approaches (prefer A):
   A) Custom claims (best):
      - Rule allows only request.auth.token.admin == true
      - Update client to refresh token after login: await auth.currentUser.getIdToken(true)
   B) Firestore role check:
      - Rule checks Firestore doc: /users/{uid}.isAdmin == true
      - Use firestore.exists + firestore.get with .data.isAdmin == true

   Provide the final rules file content and make sure it matches:
     match /driver_documents/{driverId}/{docType}/{fileName}

5) Verify Firebase project/bucket correctness:
   - Confirm storage bucket in config matches the bucket used in errors.
   - Ensure the app uses the right Firebase project (no mix of dev/prod).

6) Add a local “Rules sanity test” utility:
   - Print the exact storage path you are uploading to.
   - Print current user uid and admin claim/role before upload.
   - If unauthorized happens, show which rule condition likely failed.

Output requirements:
- Show me the exact code changes: file names + updated functions (driver create + upload service).
- Show final Storage rules file content.
- Explain briefly why 403 was happening and how your changes fix it.
- Do not ask me questions; make best assumptions and implement a working solution.
